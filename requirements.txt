# Core orchestration & blocks

prefect
prefect-snowflake

# Snowflake connectivity

snowflake-connector-python
snowflake-sqlalchemy

# HTTP

requests

# Data wrangling (optional but handy for future transforms/tests)

pandas
pyarrow

# Concurrency / retry helpers (pulled in by Prefect, listed explicitly for clarity)

tenacity
